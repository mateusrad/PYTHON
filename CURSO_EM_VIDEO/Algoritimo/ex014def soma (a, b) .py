#Crie a função soma(a,b) que recebe dois valores, soma eles e
# retornar o resultado. O nome da função deve ser "soma"
# (sem aspas e todas as letras minúsculas).
def soma(a, b):
    s = a + b
    return s


print(soma(3,2))